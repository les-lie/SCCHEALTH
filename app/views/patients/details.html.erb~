<h2>Records</h2>
<%= render partial: "shared/notice" %>
<div class="table-responsive">
  <table class="records table">
    <thead>
      <tr>
        <th>ID</th>
        <th>Initial Contact</th>
        <th>Age Range</th>
        <th>Gender</th>
	<th>Time length</th>
	<th>Participants (nos)</th>
	<th>interaction (nos)</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @patients.each do |patient| %>
        <tr>
          <td><%= patient.id %></td>
          <td><%= patient.initialcontact %></td>
          <td><%= patient.agerange %></td>
          <td><%= patient.gender %></td>
	  <td><%= patient.timelength %></td>
	  <td><%= patient.noofparticipant %></td>
	  <td><%= patient.noofinteraction %></td>

          <td><%= link_to 'Details', edit_user_path(user), class: "btn btn-primary btn-xs white" %></td>
<%
=begin %>
          <td><%= link_to 'Destroy', patient, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-xs white" if user.destroyable? %></td>
<%
=end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
<%= link_to 'Create Record', createrecord_path, class: "btn btn-primary white" %>	
